<div class="modal fade" id="confirmationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="header-modal">
        <span class="iconify" data-icon="ep:circle-close" style="color: #00586b;">
              <span aria-hidden="true">&times;</span></span>
      </div>
      <div class="body-modal">
        <p class="modal-text">Confirmez-vous un prélèvement hebdomadaire de <%= consumption.quantity * consumption.cigarette.price %> € de votre compte bancaire ?</p>
        <br>
      </div>
      <div class="body-modal">
        <p class="modal-text">Vous pouvez modifier ou annuler vos prélèvements à tout moment </p>
      </div>
      <div class="footer-modal">
        <%= form_with url: profile_bank_path, method: :patch do |f| %>
        <%= f.submit "Valider", class:"green-btn" %>
          <% end %>
      </div>
    </div>
  </div>
</div>
