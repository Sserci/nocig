<div class="background-img-objective"></div>
<div class="card-result">
  <p class="result-sentence">Un peu de volonté pour un projet<br>qui te tient à coeur :</p>
  <h2 class="result-title"> <%= current_user.objective_title %></h2>
  <div class="arrow-objective">
    <p id="start-j"><strong>J-1</strong></p>
    <img src="/assets/arrow-j" alt="">
    <p id="end-j"><strong>J-<%= current_user.objective_duration %></strong></p>
  </div>
  <p class="budget"> <%= current_user.objective_amount %>€</p>
  <p class="result-sentence">Dans <%= current_user.objective_duration%> jours, tu auras économisé <%= current_user.objective_amount%>€ en<br>évitant d'acheter <%= current_user.objective_number_of_packs %> paquets soit <%= current_user.objective_number_of_packs * 20 %> cigarettes</p>
</div>


  <%= link_to dashboard_path, class: "text-decoration-none" do %>
  <div class="btn-continuer">
    <p> Continuer</p>
  </div>
  <% end %>

  <div class="logo d-flex justify-content-center mt-4">
    <img style="height : 60px" src="/assets/Logo.png">
  </div>

<div class="footer_card d-flex align-items-center">
</div>
